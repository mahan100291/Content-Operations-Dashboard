services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.yaml
      target: development
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
